<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:statusBarColor" tools:targetApi="lollipop">@color/black</item>
    </style>

    <!-- Functional buttons style. -->
    <style name="FunctionalButtons">
        <item name="android:textSize">40sp</item>
        <item name="android:minHeight">112dp</item>
        <item name="android:maxWidth">40dp</item>
        <item name="android:textColor">@color/FuncButtonColorBackText</item>
        <item name="android:background">@drawable/func_round_buttons</item>
    </style>

    <!-- Number buttons style. -->
    <style name="NumberButtons">
        <item name="android:textSize">40sp</item>
        <item name="android:minHeight">112dp</item>
        <item name="android:maxWidth">40dp</item>
        <item name="android:textColor">@color/NumberButtonColorBackText</item>
        <item name="android:background">@drawable/num_round_buttons</item>
    </style>

</resources>
